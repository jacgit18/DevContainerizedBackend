import {FixtureTable} from "./util/index.js"

const breakdownMaterialFixtures: FixtureTable = {
  tableName: 'breakdown_material',
  alwaysFlush: true,
  data: {
  }
}

export default breakdownMaterialFixtures
